//WAP to take 10 no. Input from user find out below values
// d.Sum of Odd numbers

#include<stdio.h>
main()
{
	int num,i,oddsum=0;
	for(i=1;i<=10;i++)
	{
		printf("Enter number %d: ",i);
		scanf("%d",&num);
		if(num%2==0)
		{
			oddsum+=i;
		}
	}
	printf("Oddsum=%d",odd
	sum);
}

